
  # EduFi

  This is a code bundle for EduFi. The original project is available at https://www.figma.com/design/NCAXE4WCdrYKqL1owBR1r1/EduFi.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  